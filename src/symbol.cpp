#include <unordered_map>
#include "symbol.h"

std::unordered_map<std::string, int> var_val;   // To record the value of the value
std::unordered_map<std::string, TYPE> var_type; // To record the type of the value